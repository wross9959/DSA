```

Alg: removeInArr(A[], x, i)
	Input: Array, value to remove, size of array
	Output: Array without value x if found
	
	If (A.isEmpty() || i > A.length) return A;
	
	If (A[i] = x)
		A[i] = ''
		Return A
		
	removeArr(A[], X, i++);


```