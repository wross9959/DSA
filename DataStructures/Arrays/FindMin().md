```
Alg: findMin(A[], x, index)
	Input: 
	Output: 
	
	If (A.isEmpty() || index > length(A)) return false;
	
	If (A.length = 1) return A[index];
	
	min = minArr[A, x, index+1))
	
	If A[index] < min; return A[index]
		Return min;
		
	Return searchArr(Arr, x, index++)
	
```